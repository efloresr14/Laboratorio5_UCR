# Labo5 - IE-0117 - Estructuras de Datos
Autor: Emily Flores Rojas  
Curso: Programación Bajo Plataformas Abiertas (II-2025)

## Contenido
Implementación en C de:
- Arreglo dinámico (dynamic_array)
- Lista enlazada simple (sll)
- Lista doblemente enlazada (dll)
- Pila (stack)

Estructura del repositorio:
- `src/` : código fuente (.c)
- `include/` : headers (.h)
- `Makefile` : reglas para compilar
- `report.tex` : reporte en LaTeX
- `valgrind_output.txt` : salida de Valgrind (evidencia)

## Requisitos para evaluar
- Código modular: un .c y .h por estructura.
- Makefile funcional.
- README con instrucciones de compilación/ejecución.
- Ramas: `main`, `develop`, y al menos un `feature/*` por estructura.
- Cada feature branch con al menos 3 commits.
- Contribución de todos los miembros (si aplica).
- Código sin memory leaks (ver `valgrind_output.txt`).

## Compilar
En la raíz del repo:
```bash
make
